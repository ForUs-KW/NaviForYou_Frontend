import React from "react";
import { View, Text } from "react-native";

function report() {
  return (
    <View>
      <Button
        title="제보하기"
        onPress={()=> navigation.navigate('myPage')}
        />
    </View>
  );
}